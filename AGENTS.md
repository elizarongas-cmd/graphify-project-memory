# AGENTS.md

<!-- project-memory:start -->
## Project Memory

Start with `gpm status --project .`. Use `gpm freshness --project .` when prior context may be stale. Use `gpm query "<question>" --adaptive --project .` before broad reads; it escalates into Graphify for structural questions, empty/low-coverage memory, or truncation. Record source files actually read with `gpm source-add`, durable decisions/issues deliberately, and finish material work with a checkpoint plus `gpm report --task`. Never treat estimated context reduction as provider-billed savings. Never commit automatically without explicit user authorization.
<!-- project-memory:end -->
